import 'package:flutter/material.dart';

class SkillTrackerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Skill Tracker')),
      body: Center(child: Text('Coming soon...')),
    );
  }
}
